{
  "version": "0.56.1",
  "private": true,
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "build": "turbo build --no-daemon",
    "test": "turbo test --no-daemon",
    "tidy": "pnpm -r run --no-bail tidy",
    "update-all": "pnpm -r exec -- ncu -u --cache"
  },
  "devDependencies": {
    "npm-check-updates": "17.1.15",
    "turbo": "2.4.4"
  }
}
