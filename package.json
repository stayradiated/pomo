{
  "private": true,
  "packageManager": "pnpm@8.6.2",
  "scripts": {
    "build": "turbo build --no-daemon",
    "tidy": "turbo tidy --no-daemon"
  },
  "devDependencies": {
    "turbo": "1.10.3"
  }
}
