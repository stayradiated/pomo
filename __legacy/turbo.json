{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "tidy": {},
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["./src/**"],
      "outputs": ["./dist/**"]
    },
    "test": {
      "inputs": ["./src/**"]
    }
  }
}
