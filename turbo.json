{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "tidy": {},
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["./src/**"],
      "outputs": ["./dist/**"]
    }
  }
}
